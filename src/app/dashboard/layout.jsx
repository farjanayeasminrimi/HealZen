export const metadata = {
  title: "Patient Dashboard | HealZen",
  description: "Manage your active appointments, view clinical consultation summaries, update your personal health board, and contact doctors.",
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}
